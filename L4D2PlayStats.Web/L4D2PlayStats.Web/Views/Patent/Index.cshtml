@model L4D2PlayStats.Web.Models.PatentModel;

<h4 class="text-center">Patent system</h4>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Understanding the patent system</h3>
    </div>
    <div class="panel-body">
        <p>The patent system in our service takes into account the amount of XP that the player has earned during the games of the last <b>@Model.Config.DaysRange.ToString("N0")</b> days.</p>
        <p>
            Each victory awards the player <b>@Model.Config.Win.ToString("N0")</b> XP points, while losses cause the player to lose <b>@Math.Abs(Model.Config.Loss).ToString("N0")</b> XP points.
            An MVP SI title grants the player an additional <b>@Model.Config.Mvps.ToString("N0")</b> XP points, and an MVP CI title grants an additional <b>@Model.Config.MvpsCommon.ToString("N0")</b> XP points.
        </p>
    </div>
</div>

<table class="table table-striped table-hover table-bordered table-condensed">
    <thead>
    <tr>
        <th class="text-center">Level</th>
        <th colspan="2">Description</th>
        <th class="text-center">Experience required</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var patent in Model.Patents)
    {
        <tr>
            <td class="text-center vertical-align-middle">#@patent.Level</td>
            <td class="vertical-align-middle slim">
                <img src="@patent.Image" alt="@patent.FullName" data-toggle="tooltip" data-placement="top" title="@patent.FullName" style="width: 48px"/>
            </td>
            <td class="vertical-align-middle">
                @patent.Name
            </td>
            <td class="text-center vertical-align-middle">
                @patent.Experience.ToString("N0")
            </td>
        </tr>
    }
    </tbody>
</table>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">XP Points Breakdown</h3>
    </div>
    <div class="panel-body">
        <ul>
            <li>Victory: +@Model.Config.Win.ToString("N0") XP points</li>
            <li>Defeat: @Model.Config.Loss.ToString("N0") XP points</li>
            <li>MVP SI: +@Model.Config.Mvps.ToString("N0") XP points</li>
            <li>MVP CI: +@Model.Config.MvpsCommon.ToString("N0") XP points</li>
        </ul>
    </div>
</div>