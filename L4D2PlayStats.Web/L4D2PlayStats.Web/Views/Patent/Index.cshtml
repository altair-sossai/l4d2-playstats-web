@using L4D2PlayStats.Patent
<h4 class="text-center">Patent system</h4>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Understanding the patent system</h3>
    </div>
    <div class="panel-body">
        <p>The patent system in our service takes into account the amount of XP that the player has earned during the games of the last 45 days.</p>
        <p>Each victory awards the player 80 XP points, while losses cause the player to lose 40 XP points. An MVP SI title grants the player an additional 10 XP points, and an MVP CI title grants an additional 8 XP points.</p>
    </div>
</div>

<table class="table table-striped table-hover table-bordered table-condensed">
    <thead>
    <tr>
        <th class="text-center">Level</th>
        <th colspan="2">Description</th>
        <th class="text-center">Experience required</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var patent in PatentSystem.Patents)
    {
        <tr>
            <td class="text-center vertical-align-middle">#@patent.Level</td>
            <td class="vertical-align-middle" style="width: 1px">
                <img src="@patent.Image" alt="@patent.FullName" data-toggle="tooltip" data-placement="top" title="@patent.FullName" style="width: 48px"/>
            </td>
            <td class="vertical-align-middle">
                @patent.Name
            </td>
            <td class="text-center vertical-align-middle">
                @patent.Experience.ToString("N0")
            </td>
        </tr>
    }
    </tbody>
</table>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">XP Points Breakdown</h3>
    </div>
    <div class="panel-body">
        <ul>
            <li>Victory: +80 XP points</li>
            <li>Defeat: -40 XP points</li>
            <li>MVP SI: +10 XP points</li>
            <li>MVP CI: +8 XP points</li>
        </ul>
    </div>
</div>